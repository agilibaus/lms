RewriteEngine On

# Se il file o la directory richiesti non esistono fisicamente,
# passa tutto al front controller.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]
